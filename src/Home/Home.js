import React from "react";

import AppBar from './AppBar';


class Home extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }
  render(){
    return (
      <AppBar></AppBar>
    );
  }
}

export default Home;